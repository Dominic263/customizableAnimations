import XCTest
@testable import customizableAnimations

final class customizableAnimationsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(customizableAnimations().text, "Hello, World!")
    }
}
